{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix unity-portfolio",
    "build": "npm run build --prefix unity-portfolio",
    "preview": "npm run preview --prefix unity-portfolio",
    "install:app": "npm ci --prefix unity-portfolio"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.16"
  }
}
